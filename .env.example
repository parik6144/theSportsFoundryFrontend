# Public site (Vercel) — email only, no database required for enquiry forms.
# Optional: add DATABASE_URL only if you want enquiries saved to MySQL (EC2 admin).

SITE_URL="http://localhost:3001"
ENQUIRY_TO_EMAIL="info@sportsfoundry.in"
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER="info@sportsfoundry.in"
SMTP_PASS=""
SMTP_FROM="The Sports Foundry <info@sportsfoundry.in>"

# Optional — only for admin / saving enquiries to DB:
# DATABASE_URL="mysql://user:pass@host:3306/db_thesportsfoundry"
