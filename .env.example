# Laravel API base URL (no trailing slash)
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000/api/v1

# Optional: only needed for legacy Prisma API routes (not required on Vercel)
# DATABASE_URL="mysql://user:password@127.0.0.1:3306/db_thesportsfoundry"
